{
  "name": "glooko-data-web-app-api",
  "version": "1.0.0",
  "description": "Azure Functions API for GlookoDataWebApp",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2",
    "@azure/functions": "^4.6.0",
    "@azure/identity": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "main": "dist/src/functions/*.js"
}
