# Google OAuth Configuration for Local Development
# Copy this file to .env.local and fill in your values

# Google OAuth Client ID (obtained from Google Cloud Console)
# This is safe to use in client-side code
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Note: GOOGLE_CLIENT_SECRET should NEVER be in this file
# The client secret is only used in the backend (Azure Functions)
# and should be configured in api/local.settings.json
